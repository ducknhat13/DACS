web: php artisan serve --host=0.0.0.0 --port=${PORT:-8000}

# Procfile cho Render deployment
# Render sẽ tự động sử dụng startCommand trong render.yaml
# Nếu không dùng Blueprint, Render sẽ dùng command này

